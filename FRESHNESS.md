# SteamQHPP — Data Freshness Check

> Generated by `freshness.py` (workflow `4.3`), **daily at 07:00 UTC**. Do not edit by hand — the next run overwrites it. Companion to [COVERAGE.md](COVERAGE.md): coverage answers *how much do we have*, this answers *when was it last touched, when is it touched next, and where is that gap too big*. Design notes: ARCHITECTURE §11.6.

**Snapshot generated (UTC):** 2026-08-30 12:40  
**Base universe (`games.json`):** 127,961 games  
**Scheduled tasks:** 11 on time · 2 late · 0 stalled · 1 fill-only (no refresh cycle)

## Alerts

- 🔵 **2.6 SteamSpy tags has no refresh cycle at all** — `tags.json` is written only while unresolved games remain (0 left of 127,961); once that frontier drains the file stops changing and its data quietly freezes. Last write 25m ago. This is the pipeline's structural freshness gap — not a broken run — and closing it needs a per-entry `scraped_at` plus a rescrape cadence (COVERAGE.md → Future work).
- 🟡 **2.7 PICS metadata missed a run** — one scheduled fire passed without re-stamping `pics_raw/` (last write 1d 2h ago, 1d cadence). One miss is usually scheduler drift or a run that wrapped up on its time budget; worth a look only if it repeats.
- 🟡 **4.3 Freshness doc (this file) missed a run** — one scheduled fire passed without re-stamping `FRESHNESS.md` (last write 23h 39m ago, 1d cadence). One miss is usually scheduler drift or a run that wrapped up on its time budget; worth a look only if it repeats.
- 🟡 **2.3 Playtime raw (shards) backlog** — 57,868 of 81,117 rows (71%) are past their refresh window (proxy-inflated — see †).
- 🔴 **2.4 Update events (shards) backlog** — 52,544 of 80,671 rows (65%) are past their own refresh window. The job is running but not keeping up with its per-game promise (7d active / 45d dormant).

---

## 1. Schedule freshness — when each task last ran and when it runs next

`Last refresh` is the **data-side** stamp (the `generated_at` the task's own output carries), not "the workflow reported success" — a green run that wrote nothing shows up here as an ageing file. `Cadence` is the **worst-case** scheduled wait implied by the cron. `Gap (last → next)` is the headline: how stale the file will be, at worst, by the time the task next gets to touch it.

| # | Task → file | Cron (UTC) | Cadence | Last refresh (UTC) | Age | Missed | Next run (UTC) | Gap (last → next) | Status |
|---|---|---|---|---|---:|---:|---|---:|---|
| 1 | Catalog scrape → `games.json` | `0 0,6,12,18 * * *` | 6h | 2026-08-30 12:29 | 11m | 0 | 2026-08-30 18:00 (in 5h 19m) | 5h 30m | 🟢 on time |
| 2.1 | Prices + sale dates → `prices.json` | `7 */3 * * *` | 3h | 2026-08-30 12:34 | 6m | 0 | 2026-08-30 15:07 (in 2h 26m) | 2h 32m | 🟢 on time |
| 2.2 | 30-day review scores → `recent.json` | `41 4,10,16,22 * * *` | 6h | 2026-08-30 12:30 | 10m | 0 | 2026-08-30 16:41 (in 4h) | 4h 10m | 🟢 on time |
| 2.3 | Playtime raw (shards) → `playtime_raw/` | `23 1,4,7,10,13,16,19,22 * * *` | 3h | 2026-08-30 10:03 | 2h 37m | 0 | 2026-08-30 13:23 (in 42m) | 3h 19m | 🟢 on time |
| 2.4 | Update events (shards) → `updates_raw/` | `53 2,8,14,20 * * *` | 6h | 2026-08-30 09:24 | 3h 16m | 0 | 2026-08-30 14:53 (in 2h 12m) | 5h 28m | 🟢 on time |
| 2.5 | HLTB completion times → `hltb.json` | `53 */2 * * *` | 2h | 2026-08-30 12:36 | 4m | 0 | 2026-08-30 12:53 (in 12m) | 16m | 🟢 on time |
| 2.6 | SteamSpy tags → `tags.json` | `29 */2 * * *` | 2h | 2026-08-30 12:15 | 25m | n/a | 2026-08-30 14:29 (in 1h 48m) | 2h 13m | 🔵 fill-only |
| 2.7 | PICS metadata → `pics_raw/` | `20 3 * * *` | 1d | 2026-08-29 10:10 | 1d 2h | 1 | 2026-08-31 03:20 (in 14h 39m) | 1d 17h | 🟡 late |
| 3.1 | Playtime medians (chained) → `playtime.json` | chained to 2.3 · `23 1,4,7,10,13,16,19,22 * * *` | 3h | 2026-08-30 10:04 | 2h 36m | 0 | 2026-08-30 13:23 (in 42m) | 3h 18m | 🟢 on time |
| 3.2 | Playtime-weighted ratings (chained) → `ratings.json` | chained to 2.3 · `23 1,4,7,10,13,16,19,22 * * *` | 3h | 2026-08-30 10:05 | 2h 35m | 0 | 2026-08-30 13:23 (in 42m) | 3h 17m | 🟢 on time |
| 3.3 | Update events summary (chained) → `updates.json` | chained to 2.4 · `53 2,8,14,20 * * *` | 6h | 2026-08-30 09:24 | 3h 16m | 0 | 2026-08-30 14:53 (in 2h 12m) | 5h 28m | 🟢 on time |
| 4.1 | Shard health doc → `SHARDS.md` | `35 6 * * *` | 1d | 2026-08-30 12:13 | 27m | 0 | 2026-08-31 06:35 (in 17h 54m) | 18h 22m | 🟢 on time |
| 4.2 | Coverage doc → `COVERAGE.md` | after task 1 · `0 0,6,12,18 * * *` | 6h | 2026-08-30 06:09 | 6h 31m | 0 | 2026-08-30 18:00 (in 5h 19m) | 11h 51m | 🟢 on time |
| 4.3 | Freshness doc (this file) → `FRESHNESS.md` | `0 7 * * *` | 1d | 2026-08-29 13:01 | 23h 39m | 1 | 2026-08-31 07:00 (in 18h 19m) | 1d 17h | 🟡 late |
| 0 | Pages deploy → `(site)` | `0,30 * * * *` | 30m | — | — | — | 2026-08-30 13:00 (in 19m) | — | — |

**Missed** = scheduled fires that came and went without the output file being re-stamped, each given the job's own `timeout-minutes` as grace so a long pass still in flight never counts against it (playtime may legitimately stamp 5.5h after its cron). 🟢 0 · 🟡 1 · 🔴 2+. This is a truer gate than an age-vs-cadence ratio, which lets a daily job skip a whole run and still look fine. 🔵 **fill-only** marks a task that writes only while unfilled rows remain — silence there is expected, so it is exempt from the gate and called out in Alerts instead.

*Caveat: a file is only re-stamped when its task actually writes it. Every scraper here rewrites its `generated_at` on each successful pass, so a miss really does mean "no successful write" — but it says nothing about whether the run failed loudly or just wrapped up on its time budget. The Actions tab has that answer.*

---

## 2. How much is up to date, how much is pending

Per-game rows split by **each scraper's own gate** (constants imported from `coverage.py`, so this can't drift from COVERAGE.md). **Up to date** = inside its refresh window. **Pending refresh** = past the window, the real backlog. **Pending fill** = never fetched, the frontier. **Skipped by design** = correctly not scraped (below the 10-review floor, or a `last_modified`-driven game).

| # | Task | Store | Covered | Up to date | % | Pending refresh | Pending fill | Skipped by design |
|---|---|---|---:|---:|---:|---:|---:|---:|
| 1 | Catalog scrape | `games.json` | 25,726 | 24,900 | 96.8% | 826 | 0 | 102,235 |
| 2.1 | Prices + sale dates | `prices.json` | 86,398 | 86,398 | 100.0% | 0 | 0 | 0 |
| 2.2 | 30-day review scores | `recent.json` | 127,954 | 124,607 | 97.4% | 3,347 | 7 | 0 |
| 2.3 | Playtime raw (shards) | `playtime_raw/` | 81,117 | 23,249 | 28.7% | 57,868 † | 26 | 46,818 |
| 2.4 | Update events (shards) | `updates_raw/` | 80,671 | 28,127 | 34.9% | 52,544 | 472 | 46,818 |
| 2.5 | HLTB completion times | `hltb.json` | 127,961 | 127,960 | 100.0% | 1 | 0 | 0 |
| 2.7 | PICS metadata | `pics_raw/` | 127,897 | 127,679 | 99.8% | 218 | 64 | 0 |

**† Playtime figures are proxy-based.** `playtime_raw/` stores no per-game scrape stamp, so staleness falls back to each game's newest review `ts`: a game nobody has reviewed lately reads as overdue even if the scraper walked it this morning. Treat its `pending refresh` as an **upper bound** (contrast 2.4/2.7, which carry real per-game stamps and are exact).

---

## 3. Per-game wait — where the gap actually gets big

A task running every 3h does **not** mean every game is 3h fresh. Each scraper spends its budget on a queue, and the two-track ones park dormant games on a long cooldown *by design*. This table puts the promised per-game window next to what the data actually looks like: **p50** = typical row, **p95** = the tail, **oldest** = worst row in the file. p95 far past the window is where the gap is too big.

| # | Store | Per-game refresh window | p50 age | p95 age | Oldest row |
|---|---|---|---:|---:|---:|
| 1 | `games.json` | age-tiered 0.25d→15d (≤365d old); older = `last_modified`-driven | 52d 19h | 60d 6h | 65d |
| 2.1 | `prices.json` | no per-game gate — full non-free base re-batched every run (24h observation window) | 36m | 36m | 36m |
| 2.2 | `recent.json` | 4d active / 30d dormant | 27d 10h | 29d 21h | 30d 10h |
| 2.3 | `playtime_raw/` † | 7d active / 30d dormant | 54d 11h | 737d | 3752d |
| 2.4 | `updates_raw/` | 7d active / 45d dormant | 46d 7h | 52d 20h | 53d 6h |
| 2.5 | `hltb.json` | partial 14d / full 365d; blanks back off 3→30→180d | 29d 3h | 52d 22h | 61d |
| 2.7 | `pics_raw/` | flat 14d (`--stale-days`) | 9d 8h | 9d 8h | 14d 8h |

Row-age distributions cover **every row in the file**, including rows the window column deliberately excludes — `games.json`'s p50 is dominated by the 100k `last_modified`-driven games that are on no cooldown at all. The window describes the gate; the percentiles describe the file.

**Blind spots — data files with no per-game staleness signal at all:**

- **2.6 `tags.json`** — **none** — no per-entry timestamp, no rescrape gate. Coverage-only: 127,927 non-empty entries of 127,961 recorded. **Fill-only**: `tags_refresh.py` resolves games that have no entry yet and commits nothing when it finds none, so a quiet run is expected behaviour, not a miss — which is why this row is exempt from the missed-fire gate. Per-game freshness is unmeasurable until a `scraped_at` is stored (COVERAGE.md → Future work).

Not in this table by design: `playtime.json`, `ratings.json`, `updates.json`, `SHARDS.md`, `COVERAGE.md`, `FRESHNESS.md`, `(site)` — derived files are rebuilt wholesale from their parent (so their freshness *is* the parent's row above), and the monitor docs plus the Pages deploy hold no per-game rows. Their own timing is table 1.

---

## 4. Task notes

- **1 Catalog scrape** (`scrape.yml` → `scraper.py`): Only finder of new games. `skipped` = games past the last age tier, refreshed on Steam's `last_modified` instead of a cooldown.
- **2.1 Prices + sale dates** (`prices.yml` → `price_and_sale.py`): Fastest-moving layer; rows are re-stamped wholesale each pass, so `pending` here means a run wrapped up on its time budget before reaching them.
- **2.3 Playtime raw (shards)** (`playtime-raw.yml` → `playtime_refresh.py`): † Staleness is a PROXY (newest review `ts`, no per-game scrape stamp) — `pending refresh` is an upper bound, not real backlog.
- **2.4 Update events (shards)** (`updates.yml` → `updates_refresh.py`): 64/64 shards populated.
- **2.5 HLTB completion times** (`hltb.yml` → `hltb_refresh.py`): Blank rows awaiting retry: 34 active, 93,836 frozen (both inside their window).
- **2.6 SteamSpy tags** (`tags.yml` → `tags_refresh.py`): Coverage-only: 127,927 non-empty entries of 127,961 recorded. **Fill-only**: `tags_refresh.py` resolves games that have no entry yet and commits nothing when it finds none, so a quiet run is expected behaviour, not a miss — which is why this row is exempt from the missed-fire gate. Per-game freshness is unmeasurable until a `scraped_at` is stored (COVERAGE.md → Future work).
- **2.7 PICS metadata** (`pics.yml` → `pics_refresh.py`): 64/64 shards populated; real per-game `_ts`, so `pending refresh` is exact. Separate CM rate surface, not the storefront budget.
- **3.1 Playtime medians (chained)** (`playtime-raw.yml` → `playtime_summarize.py`): Derived — freshness inherits from `playtime_raw/`.
- **3.2 Playtime-weighted ratings (chained)** (`playtime-raw.yml` → `ratings_summarize.py`): Derived — freshness inherits from `playtime_raw/`.
- **3.3 Update events summary (chained)** (`updates.yml` → `updates_summarize.py`): Derived — freshness inherits from `updates_raw/`.
- **4.2 Coverage doc** (`coverage.yml` → `coverage.py`): Triggered by `workflow_run` after task 1 succeeds — its cadence IS the scrape cadence. A stalled row here usually means the `workflow_run` name link broke, not that the scrape stopped (that exact bug bit once — §4).
- **4.3 Freshness doc (this file)** (`freshness.yml` → `freshness.py`): Self-report: the stamp in table 1 is the PREVIOUS run's — this run has not written the file yet at the moment the row is computed.
- **0 Pages deploy** (`pages.yml` → `—`): Republishes current `main`. Leaves no artifact in the repo, so there is no local stamp to read — schedule only.

---

## How to read this

**Two different clocks, kept apart on purpose.** Table 1 measures the *job*: is the cron firing and is the file being written. Table 3 measures the *game*: how long an individual title waits for its turn. They fail in different ways and want different fixes — a stalled job is a broken pipeline (check Actions), while a healthy job with a blown p95 is a budget shortfall (raise the run minutes, widen the slots, or accept the tail).

**A big gap is not automatically a bug.** Dormant games sit on 30–45d cooldowns because their data does not move; HLTB completion times are near-static and refresh yearly. The numbers to react to are the ones this doc flags in **Alerts** — a task that has missed 2+ of its own scheduled fires, or a file whose backlog crosses 25% of covered rows.

**Known blind spots.** `tags.json` has no timestamp at all, so it cannot appear in tables 2–3 — tags are fetched once and never rechecked. `playtime_raw/` has no per-game stamp and uses a review-`ts` proxy. Both are tracked in COVERAGE.md → Future work; adding a per-entry `scraped_at` to either one would upgrade its row here from approximate to exact.

