# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-08-02 09:57
**Base universe (`games.json`):** 125,226 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-08-02 09:47 |
| `prices.json` | 2026-08-02 09:27 |
| `hltb.json` | 2026-08-02 09:23 |
| `recent.json` | 2026-08-02 09:40 |
| `playtime.json` | 2026-08-02 05:17 |
| `ratings.json` | 2026-08-02 05:17 |
| `tags.json` | 2026-08-02 04:11 |
| `updates.json` | 2026-08-02 05:56 |
| `playtime_raw/` (shards) | newest 2026-08-02 09:39 · oldest 2026-08-01 17:40 |
| `updates_raw/` (64/64 populated) | newest 2026-08-02 05:56 · oldest 2026-07-17 16:44 |
| `pics_raw/` (64/64 populated) | newest 2026-08-02 06:14 · oldest 2026-07-30 06:03 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 125,224 | 100.0% | 2 |
| Recent reviews | `recent.json` | 125,220 | 100.0% | 6 |
| HLTB (total) | `hltb.json` | 125,220 | 100.0% | 6 |
| PICS metadata (raw) | `pics_raw/` | 125,206 | 100.0% | 20 |
| PICS metadata (summ.) | `pics/` | 125,206 | 100.0% | 20 |
| Tags (non-empty) | `tags.json` | 125,186 | 100.0% | 40 |
| Review count | `games.json` | 117,464 | 93.8% | 7,762 |
| Rating % | `games.json` | 114,094 | 91.1% | 11,132 |
| Price / Sales | `prices.json` | 106,965 | 85.4% | — |
| HLTB (real) | `hltb.json` | 106,200 | 84.8% | — |
| Playtime raw (shards) | `playtime_raw/` | 79,821 | 63.7% | 45,405 |
| Playtime (summarized) | `playtime.json` | 79,692 | 63.6% | 45,534 |
| Playtime-weighted rating | `ratings.json` | 79,687 | 63.6% | 45,539 |
| Update events (raw) | `updates_raw/` | 79,291 | 63.3% | 45,935 |
| Update events (summ.) | `updates.json` | 51,551 | 41.2% | 73,675 |
| HLTB (estimated) | `hltb.json` | 19,020 | 15.2% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,651 | 113,569 | 8,481 | 0 | 6 |
| Playtime raw (approx) | `playtime_raw/` | 7,934 | 71,733 | 57,632 † | 45,432 | 127 |
| Update events | `updates_raw/` | 7,602 | 71,663 | 6,430 | 45,432 | 529 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 45,432 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 24,510 · overdue 22 · never 0 · last-modified-only 100,694. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,802 · overdue 3 · blank-frozen 81,939 · blank-active 9,476. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 125,206 · overdue 0 · never 20. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 125,206 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 125,206 | 100.0% | genre ID (structural) |
| feature categories | 125,186 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 125,144 | 100.0% | ranked community tag IDs |
| genres | 125,079 | 99.9% | genre IDs |
| supported languages | 125,139 | 99.9% | language codes |
| developer | 125,039 | 99.9% | structured dev name(s) |
| publisher | 124,749 | 99.6% | structured publisher name(s) |
| release date | 123,511 | 98.6% | PICS steam_release_date |
| release state | 123,756 | 98.8% | released/prerelease (structural) |
| review score | 79,568 | 63.5% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 54,975 | 43.9% | carriers only |
| controller support | 42,843 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 33,916 | 27.1% | Deck-rated titles only |
| franchise | 29,388 | 23.5% | structured franchise name(s) |
| content descriptors | 28,379 | 22.7% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 11,985 | 9.6% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 13,320 | 10.6% | aicontenttype 1/2 carriers only |
| custom EULA | 11,067 | 8.8% | carriers only |
| original release date | 11,214 | 9.0% | EA->1.0 carriers only |
| metacritic | 4,079 | 3.3% | carriers only |
| family-share excluded | 821 | 0.7% | excluded titles only |
| review-bomb adjusted | 97 | 0.1% | bombed titles only |

**AI content disclosure.** 13,320 games carry an `aicontenttype` flag (10.6% of catalog): **12,894 pre-generated** (shipped assets made with AI) and **426 live-generated** (runtime generation). The other ~89% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 33,916 Deck-rated titles: **8,191 Verified**, **19,551 Playable**, **6,174 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **106,965 rows (85.4% of catalog)**, tracking the non-free base (**106,975** games after removing 18,251 `is_free`). **8,775 of 106,965 priced titles (8.2%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **79,821 games (63.7% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **79,794 games (63.7% of catalog)** — against which raw is **100.0% of addressable**. The other **45,432 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 79,821 · summarized 79,692 · rated 79,687 (current gap 129). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **51,551 games (41.2%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,020 of 125,220 `hltb.json` entries carry the `est` flag** (15.2%), leaving **106,200 reals (84.8%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **114,094 games (91.1%)**; review count for **117,464 (93.8%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **125,186 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

