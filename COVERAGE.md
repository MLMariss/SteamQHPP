# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-08-18 09:06
**Base universe (`games.json`):** 126,643 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-08-18 08:46 |
| `prices.json` | 2026-08-18 08:06 |
| `hltb.json` | 2026-08-18 07:51 |
| `recent.json` | 2026-08-18 05:50 |
| `playtime.json` | 2026-08-18 06:59 |
| `ratings.json` | 2026-08-18 07:00 |
| `tags.json` | 2026-08-18 07:32 |
| `updates.json` | 2026-08-18 04:00 |
| `playtime_raw/` (shards) | newest 2026-08-18 08:39 · oldest 2026-08-17 23:11 |
| `updates_raw/` (64/64 populated) | newest 2026-08-18 04:00 · oldest 2026-07-21 16:50 |
| `pics_raw/` (64/64 populated) | newest 2026-08-18 04:04 · oldest 2026-08-16 04:05 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 126,641 | 100.0% | 2 |
| HLTB (total) | `hltb.json` | 126,640 | 100.0% | 3 |
| Recent reviews | `recent.json` | 126,632 | 100.0% | 11 |
| PICS metadata (raw) | `pics_raw/` | 126,618 | 100.0% | 25 |
| PICS metadata (summ.) | `pics/` | 126,618 | 100.0% | 25 |
| Tags (non-empty) | `tags.json` | 126,604 | 100.0% | 39 |
| Review count | `games.json` | 118,694 | 93.7% | 7,949 |
| Rating % | `games.json` | 115,291 | 91.0% | 11,352 |
| Price / Sales | `prices.json` | 108,229 | 85.5% | — |
| HLTB (real) | `hltb.json` | 107,630 | 85.0% | — |
| Playtime raw (shards) | `playtime_raw/` | 80,573 | 63.6% | 46,070 |
| Playtime (summarized) | `playtime.json` | 80,555 | 63.6% | 46,088 |
| Playtime-weighted rating | `ratings.json` | 80,550 | 63.6% | 46,093 |
| Update events (raw) | `updates_raw/` | 80,169 | 63.3% | 46,474 |
| Update events (summ.) | `updates.json` | 52,244 | 41.3% | 74,399 |
| HLTB (estimated) | `hltb.json` | 19,010 | 15.0% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,790 | 114,842 | 130 | 0 | 11 |
| Playtime raw (approx) | `playtime_raw/` | 7,995 | 72,537 | 50,014 † | 46,100 | 11 |
| Update events | `updates_raw/` | 7,721 | 72,418 | 6,530 | 46,100 | 404 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 46,100 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 24,972 · overdue 133 · never 0 · last-modified-only 101,538. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,934 · overdue 32 · blank-frozen 92,645 · blank-active 29. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 126,516 · overdue 102 · never 25. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 126,618 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 126,618 | 100.0% | genre ID (structural) |
| feature categories | 126,599 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 126,555 | 100.0% | ranked community tag IDs |
| genres | 126,490 | 99.9% | genre IDs |
| supported languages | 126,549 | 99.9% | language codes |
| developer | 126,449 | 99.9% | structured dev name(s) |
| publisher | 126,156 | 99.6% | structured publisher name(s) |
| release date | 124,837 | 98.6% | PICS steam_release_date |
| release state | 125,168 | 98.9% | released/prerelease (structural) |
| review score | 80,345 | 63.5% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 55,487 | 43.8% | carriers only |
| controller support | 43,333 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 34,211 | 27.0% | Deck-rated titles only |
| franchise | 29,628 | 23.4% | structured franchise name(s) |
| content descriptors | 28,675 | 22.6% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 12,113 | 9.6% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 13,940 | 11.0% | aicontenttype 1/2 carriers only |
| custom EULA | 11,144 | 8.8% | carriers only |
| original release date | 11,215 | 8.9% | EA->1.0 carriers only |
| metacritic | 4,090 | 3.2% | carriers only |
| family-share excluded | 789 | 0.6% | excluded titles only |
| review-bomb adjusted | 102 | 0.1% | bombed titles only |

**AI content disclosure.** 13,940 games carry an `aicontenttype` flag (11.0% of catalog): **13,501 pre-generated** (shipped assets made with AI) and **439 live-generated** (runtime generation). The other ~89% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 34,211 Deck-rated titles: **8,296 Verified**, **19,705 Playable**, **6,210 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **108,229 rows (85.5% of catalog)**, tracking the non-free base (**108,250** games after removing 18,393 `is_free`). **5,776 of 108,229 priced titles (5.3%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **80,573 games (63.6% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **80,543 games (63.6% of catalog)** — against which raw is **100.0% of addressable**. The other **46,100 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 80,573 · summarized 80,555 · rated 80,550 (current gap 18). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **52,244 games (41.3%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,010 of 126,640 `hltb.json` entries carry the `est` flag** (15.0%), leaving **107,630 reals (85.0%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **115,291 games (91.0%)**; review count for **118,694 (93.7%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **126,604 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

