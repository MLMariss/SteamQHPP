# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-08-29 07:16
**Base universe (`games.json`):** 127,911 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-08-29 07:01 |
| `prices.json` | 2026-08-29 02:44 |
| `hltb.json` | 2026-08-29 01:42 |
| `recent.json` | 2026-08-29 06:11 |
| `playtime.json` | 2026-08-29 02:18 |
| `ratings.json` | 2026-08-29 02:18 |
| `tags.json` | 2026-08-29 03:06 |
| `updates.json` | 2026-08-29 00:33 |
| `playtime_raw/` (shards) | newest 2026-08-29 02:17 · oldest 2026-08-28 02:38 |
| `updates_raw/` (64/64 populated) | newest 2026-08-29 00:33 · oldest 2026-08-12 04:52 |
| `pics_raw/` (64/64 populated) | newest 2026-08-28 15:29 · oldest 2026-08-28 15:29 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 127,909 | 100.0% | 2 |
| Recent reviews | `recent.json` | 127,887 | 100.0% | 24 |
| HLTB (total) | `hltb.json` | 127,867 | 100.0% | 44 |
| Tags (non-empty) | `tags.json` | 127,853 | 100.0% | 58 |
| PICS metadata (raw) | `pics_raw/` | 127,743 | 99.9% | 168 |
| PICS metadata (summ.) | `pics/` | 127,743 | 99.9% | 168 |
| Review count | `games.json` | 119,753 | 93.6% | 8,158 |
| Rating % | `games.json` | 116,272 | 90.9% | 11,639 |
| Price / Sales | `prices.json` | 109,354 | 85.5% | — |
| HLTB (real) | `hltb.json` | 108,846 | 85.1% | — |
| Playtime raw (shards) | `playtime_raw/` | 81,090 | 63.4% | 46,821 |
| Playtime (summarized) | `playtime.json` | 81,078 | 63.4% | 46,833 |
| Playtime-weighted rating | `ratings.json` | 81,072 | 63.4% | 46,839 |
| Update events (raw) | `updates_raw/` | 80,659 | 63.1% | 47,252 |
| Update events (summ.) | `updates.json` | 52,654 | 41.2% | 75,257 |
| HLTB (estimated) | `hltb.json` | 19,021 | 14.9% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,991 | 115,896 | 253 | 0 | 24 |
| Playtime raw (approx) | `playtime_raw/` | 8,052 | 72,988 | 57,091 † | 46,807 | 64 |
| Update events | `updates_raw/` | 7,771 | 72,850 | 45,873 | 46,807 | 483 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 46,807 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 25,680 · overdue 17 · never 0 · last-modified-only 102,214. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 32,930 · overdue 1,145 · blank-frozen 93,445 · blank-active 347. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 127,440 · overdue 303 · never 168. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 127,743 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 127,739 | 100.0% | genre ID (structural) |
| feature categories | 127,720 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 127,680 | 100.0% | ranked community tag IDs |
| genres | 127,616 | 99.9% | genre IDs |
| supported languages | 127,674 | 99.9% | language codes |
| developer | 127,571 | 99.9% | structured dev name(s) |
| publisher | 127,278 | 99.6% | structured publisher name(s) |
| release date | 125,885 | 98.5% | PICS steam_release_date |
| release state | 126,293 | 98.9% | released/prerelease (structural) |
| review score | 80,977 | 63.4% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 55,920 | 43.8% | carriers only |
| controller support | 43,718 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 34,533 | 27.0% | Deck-rated titles only |
| franchise | 29,837 | 23.4% | structured franchise name(s) |
| content descriptors | 28,949 | 22.7% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 12,204 | 9.6% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 14,446 | 11.3% | aicontenttype 1/2 carriers only |
| custom EULA | 11,217 | 8.8% | carriers only |
| original release date | 11,215 | 8.8% | EA->1.0 carriers only |
| metacritic | 4,104 | 3.2% | carriers only |
| family-share excluded | 763 | 0.6% | excluded titles only |
| review-bomb adjusted | 101 | 0.1% | bombed titles only |

**AI content disclosure.** 14,446 games carry an `aicontenttype` flag (11.3% of catalog): **13,990 pre-generated** (shipped assets made with AI) and **456 live-generated** (runtime generation). The other ~89% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 34,533 Deck-rated titles: **8,392 Verified**, **19,885 Playable**, **6,256 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **109,354 rows (85.5% of catalog)**, tracking the non-free base (**109,383** games after removing 18,528 `is_free`). **11,915 of 109,354 priced titles (10.9%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **81,090 games (63.4% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **81,104 games (63.4% of catalog)** — against which raw is **100.0% of addressable**. The other **46,807 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 81,090 · summarized 81,078 · rated 81,072 (current gap 12). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **52,654 games (41.2%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,021 of 127,867 `hltb.json` entries carry the `est` flag** (14.9%), leaving **108,846 reals (85.1%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **116,272 games (90.9%)**; review count for **119,753 (93.6%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **127,853 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

