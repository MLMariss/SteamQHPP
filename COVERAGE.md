# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-08-12 14:57
**Base universe (`games.json`):** 126,061 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-08-12 14:39 |
| `prices.json` | 2026-08-12 14:35 |
| `hltb.json` | 2026-08-12 14:35 |
| `recent.json` | 2026-08-12 11:42 |
| `playtime.json` | 2026-08-12 11:52 |
| `ratings.json` | 2026-08-12 11:53 |
| `tags.json` | 2026-08-12 14:12 |
| `updates.json` | 2026-08-12 10:05 |
| `playtime_raw/` (shards) | newest 2026-08-12 11:52 · oldest 2026-08-11 19:30 |
| `updates_raw/` (64/64 populated) | newest 2026-08-12 10:04 · oldest 2026-07-21 16:50 |
| `pics_raw/` (64/64 populated) | newest 2026-08-12 05:09 · oldest 2026-08-11 04:45 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Release date | `games.json` | 126,059 | 100.0% | 2 |
| HLTB (total) | `hltb.json` | 126,053 | 100.0% | 8 |
| Recent reviews | `recent.json` | 126,033 | 100.0% | 28 |
| Tags (non-empty) | `tags.json` | 126,018 | 100.0% | 43 |
| PICS metadata (raw) | `pics_raw/` | 126,003 | 100.0% | 58 |
| PICS metadata (summ.) | `pics/` | 126,003 | 100.0% | 58 |
| Review count | `games.json` | 118,208 | 93.8% | 7,853 |
| Rating % | `games.json` | 114,824 | 91.1% | 11,237 |
| HLTB (real) | `hltb.json` | 107,016 | 84.9% | — |
| Price / Sales | `prices.json` | 99,794 | 79.2% | — |
| Playtime raw (shards) | `playtime_raw/` | 80,284 | 63.7% | 45,777 |
| Playtime (summarized) | `playtime.json` | 80,269 | 63.7% | 45,792 |
| Playtime-weighted rating | `ratings.json` | 80,264 | 63.7% | 45,797 |
| Update events (raw) | `updates_raw/` | 79,887 | 63.4% | 46,174 |
| Update events (summ.) | `updates.json` | 52,029 | 41.3% | 74,032 |
| HLTB (estimated) | `hltb.json` | 19,037 | 15.1% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,736 | 114,297 | 68 | 0 | 28 |
| Playtime raw (approx) | `playtime_raw/` | 7,971 | 72,273 | 50,913 † | 45,798 | 19 |
| Update events | `updates_raw/` | 7,705 | 72,155 | 6,531 | 45,798 | 403 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 45,798 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 24,577 · overdue 270 · never 0 · last-modified-only 101,214. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,910 · overdue 3 · blank-frozen 92,106 · blank-active 34. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 125,905 · overdue 98 · never 58. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 126,003 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 126,003 | 100.0% | genre ID (structural) |
| feature categories | 125,984 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 125,940 | 100.0% | ranked community tag IDs |
| genres | 125,875 | 99.9% | genre IDs |
| supported languages | 125,934 | 99.9% | language codes |
| developer | 125,834 | 99.9% | structured dev name(s) |
| publisher | 125,541 | 99.6% | structured publisher name(s) |
| release date | 124,296 | 98.6% | PICS steam_release_date |
| release state | 124,553 | 98.8% | released/prerelease (structural) |
| review score | 80,106 | 63.6% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 55,268 | 43.9% | carriers only |
| controller support | 43,132 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 34,147 | 27.1% | Deck-rated titles only |
| franchise | 29,529 | 23.4% | structured franchise name(s) |
| content descriptors | 28,546 | 22.7% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 12,018 | 9.5% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 13,685 | 10.9% | aicontenttype 1/2 carriers only |
| custom EULA | 11,117 | 8.8% | carriers only |
| original release date | 11,214 | 8.9% | EA->1.0 carriers only |
| metacritic | 4,087 | 3.2% | carriers only |
| family-share excluded | 787 | 0.6% | excluded titles only |
| review-bomb adjusted | 102 | 0.1% | bombed titles only |

**AI content disclosure.** 13,685 games carry an `aicontenttype` flag (10.9% of catalog): **13,253 pre-generated** (shipped assets made with AI) and **432 live-generated** (runtime generation). The other ~89% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 34,147 Deck-rated titles: **8,270 Verified**, **19,675 Playable**, **6,202 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **99,794 rows (79.2% of catalog)**, tracking the non-free base (**107,728** games after removing 18,333 `is_free`). **5,378 of 99,794 priced titles (5.4%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **80,284 games (63.7% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **80,263 games (63.7% of catalog)** — against which raw is **100.0% of addressable**. The other **45,798 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 80,284 · summarized 80,269 · rated 80,264 (current gap 15). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **52,029 games (41.3%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **19,037 of 126,053 `hltb.json` entries carry the `est` flag** (15.1%), leaving **107,016 reals (84.9%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **114,824 games (91.1%)**; review count for **118,208 (93.8%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **126,018 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

