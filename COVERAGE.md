# SteamQHPP — Data Coverage Snapshot

> Generated by `coverage.py` after each scrape. Do not edit by hand — changes will be overwritten on the next run. Design rationale for both axes is in ARCHITECTURE §11.5.

**Snapshot generated (UTC):** 2026-07-30 12:41
**Base universe (`games.json`):** 124,878 games

Per-file generation timestamps at snapshot time:

| File | Generated (UTC) |
|---|---|
| `games.json` | 2026-07-30 12:32 |
| `prices.json` | 2026-07-30 12:11 |
| `hltb.json` | 2026-07-30 12:30 |
| `recent.json` | 2026-07-30 12:31 |
| `playtime.json` | 2026-07-30 07:50 |
| `ratings.json` | 2026-07-30 07:50 |
| `tags.json` | 2026-07-30 10:44 |
| `updates.json` | 2026-07-30 11:06 |
| `playtime_raw/` (shards) | newest 2026-07-30 12:11 · oldest 2026-07-28 04:44 |
| `updates_raw/` (64/64 populated) | newest 2026-07-30 11:06 · oldest 2026-07-15 05:57 |
| `pics_raw/` (64/64 populated) | newest 2026-07-30 06:03 · oldest 2026-07-25 05:59 |

---

## Axis 1 — Coverage by metric (sorted by % of catalog, descending)

How much of the catalog we hold for each metric. "Have we got it?"

| Metric | Storage file | Covered | % of catalog | Missing |
|---|---|---:|---:|---:|
| Recent reviews | `recent.json` | 124,878 | 100.0% | 0 |
| HLTB (total) | `hltb.json` | 124,878 | 100.0% | 0 |
| Release date | `games.json` | 124,876 | 100.0% | 2 |
| PICS metadata (raw) | `pics_raw/` | 124,846 | 100.0% | 32 |
| PICS metadata (summ.) | `pics/` | 124,846 | 100.0% | 32 |
| Tags (non-empty) | `tags.json` | 124,844 | 100.0% | 34 |
| Review count | `games.json` | 117,165 | 93.8% | 7,713 |
| Rating % | `games.json` | 113,795 | 91.1% | 11,083 |
| Price / Sales | `prices.json` | 106,662 | 85.4% | — |
| HLTB (real) | `hltb.json` | 105,915 | 84.8% | — |
| Playtime raw (shards) | `playtime_raw/` | 79,623 | 63.8% | 45,255 |
| Playtime (summarized) | `playtime.json` | 79,490 | 63.7% | 45,388 |
| Playtime-weighted rating | `ratings.json` | 79,485 | 63.7% | 45,393 |
| Update events (raw) | `updates_raw/` | 79,144 | 63.4% | 45,734 |
| Update events (summ.) | `updates.json` | 51,436 | 41.2% | 73,442 |
| HLTB (estimated) | `hltb.json` | 18,963 | 15.2% | — |

Integrity: every storage file is effectively a clean subset of `games.json` (a handful of orphan keys can appear as timing artifacts between scraper commits).

---

## Axis 2 — Refresh schedule (is the pipeline keeping up?)

Covered rows split by how each row's **own scraper** will treat it on the next pass — NOT against a flat target. The **7d (active)** lane is games updated within 90d (short cooldown); the **30d (dormant)** lane is everyone else (long cooldown, refreshed rarely *by design*). Track totals **include** their overdue members. **overdue** = already past its lane's cooldown = real backlog. **never** = no data yet = fill frontier. **empty** = correctly skipped (below the 10-review floor / null score), not pending work.

| Metric | Storage | 7d-track | 30d-track | overdue | empty | never |
|---|---|---:|---:|---:|---:|---:|
| Recent reviews | `recent.json` | 11,578 | 113,300 | 3,957 | 0 | 0 |
| Playtime raw (approx) | `playtime_raw/` | 7,866 | 71,606 | 55,709 † | 45,220 | 186 |
| Update events | `updates_raw/` | 7,570 | 71,551 | 6,459 | 45,220 | 537 |

**†  Playtime `overdue` is proxy-inflated — read with caution.** `playtime_raw/` stores no per-game scrape timestamp, so staleness uses each game's **newest review `ts`** as a proxy. A game with no recent reviews reads as overdue even if the scraper walked it days ago, so this figure is an **upper bound**, not true backlog (contrast Update events, which has a real per-game `scraped_at` and shows exact overdue). The `empty` column is the 45,220 games below the 10-review floor (correctly skipped, not backlog). An exact figure needs a per-game `scraped_at` in the shards — see Future work.

Files with a single-window (not two-track) refresh rule:

- **`games.json` core** (`scraper.py`): fresh 23,376 · overdue 1,046 · never 0 · last-modified-only 100,456. Games within 365d of release are on the age-tiered review refresh (≤3d→0.25d, ≤10d→0.5d, ≤30d→1d, ≤60d→2d, ≤90d→3.5d, ≤180d→7d, ≤365d→15d), so fresh/overdue are exact for them. Older games refresh on Steam's `last_modified` (invisible locally) plus the PICS review-drift trigger, and are counted separately rather than scored against a cooldown they don't have.
- **`hltb.json`** (`hltb_refresh.py`): fresh 33,696 · overdue 3 · blank-frozen 31,164 · blank-active 60,015. Windows: partial 14d, full 365d; blanks back off 3→30→180d by attempt count.
- **`prices.json`** (`price_and_sale.py`): no cooldown gate — the whole non-free base is re-batched every 3h, so there is no meaningful staleness backlog to bucket.
- **`tags.json`** (`tags_refresh.py`): **no timestamp stored and no rescrape schedule** — coverage-only. See Future work below.
- **`playtime.json` / `ratings.json`**: derived from `playtime_raw/` on every raw pass; staleness inherits from the raw shard row above.
- **`pics_raw/`** (`pics_refresh.py`): fresh 124,846 · overdue 0 · never 32. Single flat `--stale-days 14` window (daily cron), and — unlike playtime — keyed on a **real per-game `_ts` fetch stamp**, so overdue here is exact, not a proxy. `never` = catalog games not yet in `pics_raw/` (the fill frontier). `pics/` is derived from `pics_raw/` by `pics_summarize.py` on every pass, so its staleness inherits from this row.

---

## PICS metadata — sub-metric coverage

Fill rates **within the 124,846 summarized `pics/` records** (not against the full catalog). Structural fields (tags, genres, categories, languages, dev/pub) sit near 100%; the sparse rows below them are **by-design carrier-only** fields — their % is a real-world incidence rate (how many games actually carry an AI disclosure, a Deck rating, a review-bomb adjustment, …), **not** a pipeline gap. These figures are live over the full library — they supersede the 120-game probe sample in `PICS_METADATA_PIPELINE.md §2`.

| Sub-metric | Covered | % of `pics/` | Note |
|---|---:|---:|---|
| primary_genre | 124,846 | 100.0% | genre ID (structural) |
| feature categories | 124,826 | 100.0% | single-player/co-op/cloud/... flags |
| store_tags | 124,784 | 100.0% | ranked community tag IDs |
| genres | 124,719 | 99.9% | genre IDs |
| supported languages | 124,779 | 99.9% | language codes |
| developer | 124,679 | 99.9% | structured dev name(s) |
| publisher | 124,389 | 99.6% | structured publisher name(s) |
| release date | 123,195 | 98.7% | PICS steam_release_date |
| release state | 123,396 | 98.8% | released/prerelease (structural) |
| review score | 79,504 | 63.7% | Valve bucket + %positive (review-floor bound) |
| full-audio languages | 54,848 | 43.9% | carriers only |
| controller support | 42,735 | 34.2% | full/partial (from cats 28/18) |
| Steam Deck compat | 33,899 | 27.2% | Deck-rated titles only |
| franchise | 29,316 | 23.5% | structured franchise name(s) |
| content descriptors | 28,297 | 22.7% | mature flags (adult gate = code 3 or 4) |
| Early Access (genre-70) | 11,925 | 9.6% | Valve EA signal (not the SteamSpy tag) |
| AI content disclosure | 13,171 | 10.5% | aicontenttype 1/2 carriers only |
| custom EULA | 11,054 | 8.9% | carriers only |
| original release date | 11,214 | 9.0% | EA->1.0 carriers only |
| metacritic | 4,074 | 3.3% | carriers only |
| family-share excluded | 821 | 0.7% | excluded titles only |
| review-bomb adjusted | 97 | 0.1% | bombed titles only |

**AI content disclosure.** 13,171 games carry an `aicontenttype` flag (10.5% of catalog): **12,749 pre-generated** (shipped assets made with AI) and **422 live-generated** (runtime generation). The other ~89% of `pics/` records carry no disclosure flag. PICS gives the typed flag + category only; the free-text blurb lives on the store page.

**Steam Deck.** Of the 33,899 Deck-rated titles: **8,182 Verified**, **19,546 Playable**, **6,171 Unsupported**. The long tail of the catalog is simply unrated by Valve (no Deck category), which is why Deck coverage is ~27% of `pics/`, not the ~97% the AAA-heavy probe sample suggested.

---

## Notes

**Prices.** `prices.json` holds **106,662 rows (85.4% of catalog)**, tracking the non-free base (**106,665** games after removing 18,213 `is_free`). **8,762 of 106,662 priced titles (8.2%) are currently discounted**; sale end-dates come from the `IStoreBrowseService/GetItems` pass.

**Playtime backfill.** Raw playtime covers **79,623 games (63.8% of catalog)**. The honest denominator is the addressable set after the `MIN_REVIEWS_FLOOR = 10` gate — **79,658 games (63.8% of catalog)** — against which raw is **100.0% of addressable**. The other **45,220 games are below the floor and correctly skipped** (too few reviews to produce a sentiment-split median).

**Summarizers run on every raw pass.** Raw 79,623 · summarized 79,490 · rated 79,485 (current gap 133). `playtime_summarize.py` and `ratings_summarize.py` are chained into `playtime-raw.yml`, so the small frontend files refresh right after the shards do (8×/day). Any residual gap is just within-run ordering between the shard commit and the summarize steps, not a stalled pipeline.

**Update events.** `updates.json` (event_type-based patch history) covers **51,436 games (41.2%)**, built from **64 of 64 `updates_raw/` shards populated**. This layer feeds the frontend's **Updated column cadence badge** (`N · 90d` / `N · 1y`, from the summed 90d/365d `counts`) and backfills a null News-API `last_update_ts`. It is gated by the same 10-review floor as playtime. As the remaining shards populate this coverage rises; the precedence flip (event layer becomes primary over News-API) is gated on that coverage — see ARCHITECTURE §9.5 / §3.1.

**HLTB estimates refresh live.** **18,963 of 124,878 `hltb.json` entries carry the `est` flag** (15.2%), leaving **105,915 reals (84.8%)**. `hltb_estimate.py` is a shared helper imported by `hltb_refresh.py` (as `HE`) and called in the fill loop, so estimates recompute from live median ratios every 2h as new reals land — not a stale one-off.

**Rating % / review count.** Rating % is present for **113,795 games (91.1%)**; review count for **117,165 (93.8%)**. The gap is titles with too few reviews to carry a meaningful score.

**Tags** are non-empty for **124,844 games (100.0%)**. The remainder are untagged on SteamSpy (typically very-low-review or unreleased titles).

---

## Future work

- **Tag refresh schedule.** `tags.json` currently has no per-entry timestamp and no rescrape cadence — tags are fetched once and left. Tags do drift (Steam user-tag votes shift, new tags get added), so a light periodic re-check (e.g. weekly/monthly, oldest-first, small budget) is worth adding. That needs a per-entry `scraped_at` in `tags.json` first, after which tags can join Axis 2 as a proper row here.
- **Uniform-refresh review.** Once the fill frontier drains (near-100% coverage), revisit whether the dormant 30–45d cooldowns should tighten toward a flatter, faster whole-catalog refresh. Today's split (active 4–7d / dormant 30–45d) deliberately front-loads budget onto games whose data actually moves; a uniform target only makes sense once no backlog is competing for that budget.
- **Playtime per-game timestamp.** Adding a per-game `scraped_at` to the `playtime_raw/` shard records would replace the newest-review-`ts` proxy with an exact staleness signal, removing the (approx) caveat above.

